port: 3000
dehydratedBaseDir: /data/dehydrated
enableWatcher: false
plugins:
  openssl:
    enabled: false
  # External plugins can be configured via environment variables
  # Example:
  # firewall:
  #   enabled: false
  #   path: /path/to/plugin/plugin.go
  #   config:
  #     host: fw.example.com
  #     user: admin
  #     password: password
